import { RoleDirectiveDirective } from './role-directive.directive';

describe('RoleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
